{
    "priority": [
      { "if": "average_slope > 8", "multiply_by": 0.3 },
      { "if": "average_slope > 4", "multiply_by": 0.6 },
      { "if": "average_slope < -6", "multiply_by": 1.2 },
  
      { "if": "surface == GRAVEL", "multiply_by": 0.6 },
      { "if": "surface == DIRT", "multiply_by": 0.4 },
      { "if": "surface == SAND", "multiply_by": 0.2 },
      { "if": "surface == ASPHALT", "multiply_by": 1.0 }
    ],
    "speed": [
      { "if": "average_slope > 8", "multiply_by": 0.5 },
      { "if": "average_slope > 4", "multiply_by": 0.7 },
      { "if": "average_slope < -6", "multiply_by": 1.1 },
  
      { "if": "surface == GRAVEL", "multiply_by": 0.7 },
      { "if": "surface == DIRT", "multiply_by": 0.5 },
      { "if": "surface == SAND", "multiply_by": 0.3 },
      { "if": "surface == ASPHALT", "multiply_by": 1.0 }
    ]
  }
  